{
  "name": "neon3-inventory-example",
  "private": true,
  "type": "module",
  "engines": {"node": ">=18"},
  "scripts": {"build": "tsc -p tsconfig.json", "test": "npm run build && node --test dist/test/*.test.js", "start": "npm run build && node dist/src/inventory.js", "probe": "npm run build && node dist/src/probe.js --once --diagnostic"},
  "dependencies": {"@neon3/sdk": "0.1.4"},
  "devDependencies": {"typescript": "^5.6.0", "@types/node": "^22.0.0"}
}
